import { TestBed } from '@angular/core/testing';

import { CosCenterService } from './cos-center.service';

describe('CosCenterService', () => {
  let service: CosCenterService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(CosCenterService);
  });

  it('should be created', () => {
  });
});
